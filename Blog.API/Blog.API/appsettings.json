/*
   Here we perfomed th connection string to connect the API to SQL server

  followinf the below code snippet:

  Server=myServerAddress;Database=myDatabase;User Id=myUsername;Password=myPassword;

*/


{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "BlogConnectionString": "Server=localhost\\MSSQLSERVER01;Database=BlogDb;TrustServerCertificate = True; Trusted_Connection = True" // This is the connection string to connect the API to MSSQL
  }

}
